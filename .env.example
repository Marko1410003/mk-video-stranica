# ─── Backend / SMTP ───────────────────────────────────────────
# SMTP credentials for sending contact form emails via Nodemailer.
# If any of these are blank, the backend falls back to saving
# submissions to backend/submissions.json (dev mode only).

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
# For Gmail: use an App Password (Google Account → Security → App Passwords)
SMTP_PASS=your-app-password-here

# Email address that receives contact form submissions
TO_EMAIL=mkrnic03@gmail.com

# ─── Server ───────────────────────────────────────────────────
PORT=3001

# ─── CORS ─────────────────────────────────────────────────────
# URL of the frontend (in production, replace with your real domain)
FRONTEND_ORIGIN=http://localhost:5173
